<% layout("/layouts/boilerplate") -%>

<div class="row justify-content-center mt-5 mb-3">
  <div class="col-md-6 col-lg-5">
    <!-- Card for better UI -->
    <div class="card shadow-lg rounded-4 border-0">
      <div class="card-body p-4">
        <!-- Heading -->
        <h2 class="text-center mb-4 fw-bold">
          
        <span class="gradient-text">Welcome Back </span> <span class="emoji">👋</span>
        </h2>
         

        <!-- Login Form -->
        <form method="POST" action="/login" class="needs-validation" novalidate>
          
          <!-- Username -->
          <div class="mb-3">
            <label for="username" class="form-label fw-semibold">Username</label>
            <div class="input-group">
              <span class="input-group-text bg-light"><i class="bi bi-person"></i></span>
              <input 
                name="username" 
                id="username" 
                type="text" 
                class="form-control" 
                placeholder="Enter your username" 
                required>
              <div class="invalid-feedback">Please enter your username.</div>
            </div>
          </div>

          <!-- Password -->
          <div class="mb-3">
            <label for="password" class="form-label fw-semibold">Password</label>
            <div class="input-group">
              <span class="input-group-text bg-light"><i class="bi bi-lock"></i></span>
              <input 
                name="password" 
                id="password" 
                type="password" 
                class="form-control" 
                placeholder="Enter your password" 
                required>
              <div class="invalid-feedback">Password is required.</div>
            </div>
          </div>

          <!-- Remember Me & Forgot -->
          <div class="d-flex justify-content-between align-items-center mb-3">
            <div class="form-check">
              <input class="form-check-input" type="checkbox" id="rememberMe">
              <label class="form-check-label" for="rememberMe">Remember me</label>
            </div>
            <a href="/forgot-password" class="text-decoration-none gradient-text fw-semibold">Forgot password?</a>
          </div>

          <!-- Submit Button -->
          <div class="d-grid mt-4">
            <button type="submit" class="btn gradient-btn btn-lg rounded-3 shadow-sm">
              <i class="bi bi-box-arrow-in-right"></i> Login
            </button>
          </div>

                    <!-- Divider -->
          <div class="text-center my-3">
            <span class="text-muted">or</span>
          </div>

          <!-- Google Login Button -->
          <div class="d-grid mb-3">
            <a href="/auth/google" class="btn btn-light border d-flex align-items-center justify-content-center gap-2 shadow-sm rounded-3">
              <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/google/google-original.svg" width="20">
              <span class="fw-semibold">Continue with Google</span>
            </a>
          </div>

          <!-- Signup Option -->
          <p class="text-center mt-2 mb-0">
            Don’t have an account? 
            <a href="/signup" class="text-decoration-none gradient-text fw-semibold">Sign Up</a>
          </p>
        </form>
      </div>
    </div>
  </div>
</div>

<!-- Custom CSS -->
<style>
  /* Gradient text */
  .gradient-text {
  background: linear-gradient(90deg, #0072FF 0%, #A832FF 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  display: inline-block; /* Important for emoji separation */
}


  /* Gradient button */
  .gradient-btn {
    background: linear-gradient(90deg, #0072FF 0%, #cf32ff 100%);
    color: #fff;
    border: none;
    transition: 0.3s;
  }

  .gradient-btn:hover {
    opacity: 0.9;  
  }

  
</style>
